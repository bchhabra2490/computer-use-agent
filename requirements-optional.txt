# Optional providers. Install on top of requirements-core.txt:
#   pip install -r requirements-core.txt -r requirements-optional.txt
# Raspberry Pi GPIO extras: requirements-pi.txt
sarvamai>=0.1.0
# Local TTS: Piper (CPU ONNX) and Kokoro (mlx-audio on Apple Silicon).
# Do not use misaki[en] on Python 3.14 — that extra pulls spacy 4.dev which
# fails to build. Pin the English G2P deps instead.
piper-tts>=1.3.0
mlx-audio>=0.2.0; sys_platform == "darwin"
# Phonon-1 STT (optional). fermion-research also wants torch + transformers 5
# for Neutrino LLMs; this repo stays on transformers 4, so install speech-only:
#   pip install 'fermion-research>=0.1.24' --no-deps
mlx-lm>=0.20.0; sys_platform == "darwin"
zstandard>=0.23.0; sys_platform == "darwin"
misaki>=0.7.4; sys_platform == "darwin"
num2words>=0.5.14; sys_platform == "darwin"
spacy==3.8.16; sys_platform == "darwin"
phonemizer>=3.3.0; sys_platform == "darwin"
# Optional STT_SMART_TURN local audio endpointing (no torch required).
transformers>=4.48.2
speakeronnx>=0.1.0
# Local LLM traces UI (optional). JSONL still writes without this.
# Run: phoenix serve  → http://127.0.0.1:6006  and set PHOENIX=1
arize-phoenix>=11.0.0
# TypeSafe Jev (optional fast computer-use policy). App starts without it when
# JEV_FAST_LOOP is off. Install when enabling Jev:
#   pip install 'typesafe-sdk>=0.7.0,<1'
typesafe-sdk>=0.7.0,<1
